all:
  hosts:
    services-host:
      ansible_host: 'localhost.localdomain'
      ansible_connection: 'local'
  children:
    nextcloud:
      hosts:
        services-host:
    mattermost:
      hosts:
        services-host:
    rocketchat:
      hosts:
        services-host:
    moodle:
      hosts:
        services-host:
    dokuwiki:
      hosts:
        services-host:
  vars:
    is_local_env: yes
